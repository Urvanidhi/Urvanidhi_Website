import React from "react";

const Products = () => (
  <section id="products" className="py-20 bg-green-50">
    <h2 className="text-3xl font-bold text-green-800 text-center">Products & Services</h2>
    <p className="mt-4 text-green-700 text-center max-w-3xl mx-auto">
      We offer cutting-edge agricultural products, smart farming tools,
      and consultancy services to improve yield and sustainability.
    </p>
  </section>
);

export default Products;
